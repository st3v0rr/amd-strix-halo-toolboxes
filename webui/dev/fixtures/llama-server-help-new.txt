usage: llama-server [options]

common params:
  -h,    --help, --usage          print usage and exit
  -m,    --model FNAME            model path
  -c,    --ctx-size N             size of the prompt context
  -ngl,  --n-gpu-layers N         number of layers to store in VRAM
  -fa,   --flash-attn [on|off|auto]   set Flash Attention use
         --load-mode {none,mmap,prefetch}  how to load model data
         --no-mmap                (deprecated, use --load-mode none)
         --api-key KEY            API key to use for authentication
         --jinja                  use jinja template for chat

speculative decoding:
         --spec-type TYPE,...     speculative decoding strategies (default: none)
         --spec-draft-n-max N     number of tokens to draft (default: 3)
